name: ci
on:
  push:
    branches: [develop, main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:
    inputs:
      skip_build:
        description: "Skip build stage"
        type: boolean
        default: false
      skip_test:
        description: "Skip test/lint stage"
        type: boolean
        default: false
      skip_security:
        description: "Skip security stage"
        type: boolean
        default: false
      skip_docker:
        description: "Skip docker stage"
        type: boolean
        default: false
      skip_deploy:
        description: "Skip deploy stage"
        type: boolean
        default: false

jobs:
  # Replace <YOUR-ORG>/<PLATFORM-REPO>@v1 with your platform repository and release tag.
  build:
    if: ${{ github.event_name != 'workflow_dispatch' || !inputs.skip_build }}
    uses: <YOUR-ORG>/<PLATFORM-REPO>/.github/workflows/build.yml@v1
    with: { runtime: node, runtime-version: "20", cache-dependency-path: "**/package-lock.json" }

  test:
    needs: build
    if: >-
      ${{
        always() &&
        (github.event_name != 'workflow_dispatch' || !inputs.skip_test) &&
        (needs.build.result == 'success' || needs.build.result == 'skipped')
      }}
    uses: <YOUR-ORG>/<PLATFORM-REPO>/.github/workflows/test.yml@v1
    with: { runtime: node, runtime-version: "20", cache-dependency-path: "**/package-lock.json" }

  security:
    needs: build
    if: >-
      ${{
        always() &&
        (github.event_name != 'workflow_dispatch' || !inputs.skip_security) &&
        (needs.build.result == 'success' || needs.build.result == 'skipped')
      }}
    uses: <YOUR-ORG>/<PLATFORM-REPO>/.github/workflows/security.yml@v1
    with: { runtime: node, runtime-version: "20", sbom-format: spdx-json }

  docker:
    if: >-
      ${{
        always() &&
        (github.event_name == 'push' || github.event_name == 'workflow_dispatch') &&
        (github.event_name != 'workflow_dispatch' || !inputs.skip_docker) &&
        (needs.build.result == 'success' || needs.build.result == 'skipped') &&
        (needs.test.result == 'success' || needs.test.result == 'skipped') &&
        (needs.security.result == 'success' || needs.security.result == 'skipped')
      }}
    needs: [build, test, security]
    uses: <YOUR-ORG>/<PLATFORM-REPO>/.github/workflows/docker.yml@v1
    with:
      image_name: sample-app
      registry: ghcr
      semver: ${{ startsWith(github.ref, 'refs/tags/v') && github.ref_name || '' }}

  deploy:
    if: >-
      ${{
        always() &&
        (github.event_name == 'push' || github.event_name == 'workflow_dispatch') &&
        (github.event_name != 'workflow_dispatch' || !inputs.skip_deploy) &&
        needs.docker.result == 'success' &&
        (
          github.event_name == 'workflow_dispatch' ||
          github.ref == 'refs/heads/develop' ||
          github.ref == 'refs/heads/main' ||
          startsWith(github.ref, 'refs/tags/v')
        )
      }}
    needs: docker
    uses: <YOUR-ORG>/<PLATFORM-REPO>/.github/workflows/deploy.yml@v1
    with:
      cloud: aws
      environment: ${{ startsWith(github.ref, 'refs/tags/v') && 'prod' || github.ref == 'refs/heads/main' && 'test' || 'dev' }}
      image_uri: ${{ needs.docker.outputs.image-uri }}
      service_name: sample-app
      aws-role-to-assume: ${{ vars.AWS_ROLE_TO_ASSUME }}
